{
  "coding mode": {
    "description": "Development environment",
    "aliases": ["start coding", "dev mode", "development environment", "coding environment"],
    "steps": [
      {"action": "app.open", "params": {"name": "vscode"}},
      {"action": "app.open", "params": {"name": "chrome"}},
      {"action": "app.open", "params": {"name": "terminal"}},
      {"action": "file.open_folder", "params": {"path": "~/Projects"}},
      {"action": "monitor.session", "params": {"label": "coding", "enable": true}}
    ]
  },
  "study mode": {
    "description": "Focused study session",
    "aliases": ["start studying", "focus mode"],
    "steps": [
      {"action": "app.open", "params": {"name": "chrome"}},
      {"action": "file.open_folder", "params": {"path": "~/Documents"}},
      {"action": "monitor.session", "params": {"label": "study", "enable": true}},
      {"action": "notify.send", "params": {"text": "Study session started."}}
    ]
  }
}
